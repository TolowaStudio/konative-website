name: Konative Daily Ingest — News

# DISABLED (frozen): News ingest stays off per web/AGENTS.md. Scheduled runs removed.
# Re-enable only with explicit operator approval.
#
# Legacy: daily 06:00 UTC → GET /api/ingest-news on konative.com (was Railway cron).

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT: tolowa-studio
  SITE_URL: ${{ vars.SITE_URL || 'https://konative.com' }}

on:
  workflow_dispatch: # manual trigger only — fails fast below

jobs:
  ingest:
    name: Trigger news ingestion endpoint
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Blocked — news ingest frozen
        run: |
          echo "::error::ingest-news.yml schedule removed. News ingest is frozen (web/AGENTS.md)."
          echo "::error::Do not re-enable cron or workflow_dispatch without explicit approval."
          exit 1
